<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gbk1021.gbk.mapper.getGbkFutureResult">
    <!-- 사용자 목록 조회 -->
    <select id="getGbkFutureResult" resultType="Map">
        SELECT *
        FROM gbk_future_results
        WHERE sex = #{sex}
          AND year = #{year}
          AND month = #{month}
          AND day = #{day}
          AND career = #{career}
          AND pay = #{pay}
          AND edu = #{edu}
          AND job = #{job}
    </select>

</mapper>